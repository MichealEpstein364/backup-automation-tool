# Troubleshooting

## Missing config file
Ensure \`config.conf\` exists in the repo root.

## Archive not created
Check whether \`SOURCE_DIR\` is valid and readable.

## Cron not running
Verify script paths and executable permissions.

## Runtime files missing
Run \`remote_sync.sh\` manually first and inspect \`/tmp/.backup_runtime\`.

